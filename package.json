{
  "name": "kutecula-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter ./artifacts/kutecula run build"
  },
  "dependencies": {
    "@vercel/node": "^3.2.27",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
